import styles from './OrderButton.module.css';

const OrderButton = () => {
    return (
        <div className={styles.container}>
    <button>
        Order
    </button>
    </div>
    );
};

export default OrderButton;
