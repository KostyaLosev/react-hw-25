import styles from "./mainLoginText.module.css";

const MainLoginText = () => {
    return (
    <div className={styles.container}>
        <h1 className={styles.title}>Log in</h1>
    </div>
    );
};

export default MainLoginText;